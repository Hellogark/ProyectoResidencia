Schema de admins

[Proyecto{
	Admins: {
		id: string,
		nombre: String obligatorio,
		password: String obligatorio encriptado,
		correo: String obligatorio y único,
		img: string opcional,
		fechaDeCracion: string,
		role: String - obligatorio con valor por defecto y debe exsitir en una lista de errores
		Google: boolean opcional
		
	}	1 admin puede subir muchos archivos ↓ 1 archivo puede pertenecer a 1 admin
	
	Archivos: {
		id: string,
		responsable: string,
		nombre: String,
		fechaDeCreacion: string,
		fechaModificado: string	
	}
		1 proyecto puede tener muchos archvivos 	↑
	Proyectos:{
		id: string,
		nombre: String,
		descripcion: string,
		fechaDeCreacion: string,
		fechaProyectada: string,
		nombreEmpresa: string,
			
	}		1 usuario puede subir muchos archivos 
	Usuarios{
	nombre: {type: String, required: [true, "El nombre es necesario"] } ,
    correo: {type: String, unique:true, required: [true, "El correo es necesario"] } ,
	password: {type: String,  required: [true, "La contraseña es necesaria"] } ,
		
		img: {type: String, required: false } ,
		fechaDeCracion: string,
		role: {type: String, required: true, default:'USER_ROLE'} 
	
	}
	


}]